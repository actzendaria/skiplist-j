package util;

import util.ThreadStateManager;
import util.ThreadState;
import util.SkiplistRotating;


public class BackgroundManager {
	/*
	static void bg_init(ThreadState s);
	static void bg_start(int sleeptime);
	static void bg_stop();
	static void bg_print_stats();
	static void bg_remove(Node<V> prev, Node<V> node, Threadstate thrst);
	static void bg_help_remove(Node<V> prev, Node<V> node, Threadstate thrst);
*/
}
